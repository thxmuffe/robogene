(ns robogene.frontend.db)

(def default-db
  {:status "Loading existing frames..."
   :backend-mode :frames
   :frame-inputs {}
   :last-rendered-revision nil
   :gallery-items []
   :latest-state nil})
