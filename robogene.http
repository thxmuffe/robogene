@base = http://localhost:7071
@json = application/json

### 1) Get current episode state (run this first)
#
# This request stores the response as `state`.
#
# @name state
GET {{base}}/api/state
Cache-Control: no-store

### 2) Wait for state changes (long-poll)
GET {{base}}/api/wait-state?since=0&timeoutMs=25000
Cache-Control: no-store

### 3) Queue a frame generation
POST {{base}}/api/generate-frame
Content-Type: {{json}}
Cache-Control: no-store

{
  "frameId": "replace-with-frame-id-from-state",
  "direction": "Tighten composition and keep continuity with previous frame."
}
